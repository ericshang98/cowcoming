# Cowcoming / 牛来：仓库会话入口

本规则适用于整个仓库。新会话、换设备、恢复上下文后，在设计或改代码前先读：

1. [产品文档入口](docs/product/README.md)
2. [产品文档完整快照](docs/product/spec.md)
3. [项目现状与已知决策](docs/project-context.md)
4. 涉及上线时再读 [部署说明](docs/deployment.md)。

## 产品依据

- 产品真源是 Eric 的[飞书文档](https://uxl9fceo481.feishu.cn/wiki/Ehc9w5bs1ic2gEkwZjCc8Eo1nmb)。先核对最新内容；有权限的环境可运行 `npm run docs:sync`。没有权限时读取仓库快照，明确快照修订号；不要假称已读最新文档。普通维护可以继续，涉及无法确认的产品冲突时再向 Eric 澄清。
- 开工时简短说明本次涉及哪些文档章节、当前实现与目标行为的差距。不要只根据域名、截图或旧页面推断产品。
- Eric 当前明确指令优先于旧文档。同一文档有冲突时，以明确标注的最新修正为准，记录依据；仍不明确的部分不自行编造。
- 产品文档中的规划不等于已实现。尤其不能把网页 3D 动画、模拟状态或收到请求写成机械臂已连接、动作已完成。
- 牛来成长是分支进化树，不是固定三阶段或四阶段。IP、形态、硬件能力分别建模，详见产品文档 §5 和项目现状。
- 本仓库不依赖某台机器上的个人 Skill；不要求安装或同步 Skill。规则、产品快照和交接信息均随 Git 同步。

## 开发与交付

- Node 24，`npm ci`、`npm test`、`npm run build`。入口和目录见 README。
- 新工作分支使用 `codex/` 前缀。改动前查看 `git status`，不覆盖他人未提交修改。
- 任务结束更新发生变化的项目事实、验证结果和待办；产品决定与原始飞书快照分开记录。不要手改 `docs/product/spec.md` 冒充上游内容。
- 保持凭据在各设备自己的登录状态或密钥管理中。不要提交 `.env`、Cookie、Token、Cloudflare/Lark/GitHub 登录文件；`VITE_*` 会进入浏览器，不能存密钥。
- 线上站点使用已有 Cloudflare Pages 项目 `niulai-preview-20260922`。仅 push 本仓库不会自动上线；不要另建同名站点、修改 DNS 或覆盖其他项目。部署前按部署说明核对目标和版本。
- 未经 Eric 明确授权，不联系、调度或中断其他 Agent / 会话，不修改其工作目录或占用中的服务。
- 构建、测试、提交不等于装机许可。安装或覆盖设备 App、电脑连接器、运行时，以及通过重启激活新版本，需要 Eric 对版本和目标设备明确批准。不要使用 iPhone 镜像装机或验收。

## 交接

向 Eric 说明改了什么、实际验证了什么、尚未完成什么。将长期有效的新事实写入仓库文档后提交，不能只留在聊天记录或本机绝对路径中。
